//Usar "is"Active como buena practica
const isActive=true
//Usar "has"Permission como buena practica
const hasPermisson=false

// Conversión Implícita
const result=5>3
console.log(result)

const name='Platzi'
console.log(!!name)

// Conversión explícita (el cero como false y el resto de numeros los toma como true)
const value=0
const otherValue=-24
const explicitBoolean=Boolean(otherValue)
console.log(explicitBoolean)

//